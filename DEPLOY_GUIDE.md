# JNet GitHub Pages 部署指南

## 🚀 快速部署

### 1. 本地测试

```bash
cd /Users/sanbo/code/JNet/docs
python3 -m http.server 8000
```

访问: http://localhost:8000

### 2. 功能验证

访问测试页面: http://localhost:8000/test.html

点击"运行所有测试"按钮,确保所有测试通过。

### 3. 部署到 GitHub Pages

#### 方法 1: 自动部署 (推荐)

GitHub Actions 已配置,推送代码后自动部署:

```bash
git add docs/
git commit -m "feat: 更新 GitHub Pages - 多语言和搜索功能"
git push origin main
```

#### 方法 2: 手动部署

```bash
# 确保在项目根目录
cd /Users/sanbo/code/JNet

# 推送 docs 目录到 gh-pages 分支
git subtree push --prefix docs origin gh-pages
```

### 4. 访问部署后的页面

- **GitHub Pages URL**: https://netcapture.github.io/JNet/
- **测试页面**: https://netcapture.github.io/JNet/test.html

## 📋 功能清单

### ✅ 已实现功能

#### 多语言支持
- [x] 中英文切换
- [x] URL 参数支持 (`?lang=en`)
- [x] 本地存储记忆
- [x] 浏览器语言检测
- [x] 实时内容更新

#### 搜索功能
- [x] 50+ 搜索项索引
- [x] 实时搜索
- [x] 关键词高亮
- [x] 分类统计
- [x] 键盘快捷键 (Ctrl+K)
- [x] 键盘导航 (↑↓ Enter ESC)
- [x] 移动端适配

#### UI/UX
- [x] 现代化设计
- [x] 磨砂玻璃效果
- [x] 流畅动画
- [x] 响应式布局
- [x] 测试工具

## 🔧 配置说明

### 语言配置

在 `docs/i18n.js` 中添加新翻译:

```javascript
const translations = {
    zh: {
        your_key: "中文翻译"
    },
    en: {
        your_key: "English translation"
    }
};
```

### 搜索索引配置

在 `docs/search.js` 中添加新搜索项:

```javascript
const SEARCH_INDEX = [
    {
        id: 'unique-id',
        category: 'feature',  // feature, code, performance, architecture, version, about
        title: '标题',
        titleEn: 'Title',
        content: '内容描述',
        contentEn: 'Content description',
        keywords: ['key1', 'key2'],
        link: '#section-id'  // 或完整 URL
    }
];
```

### HTML 翻译配置

在 `docs/index.html` 中添加翻译属性:

```html
<p data-i18n="your_key">默认文本</p>
```

## 🧪 测试清单

### 功能测试
- [ ] 语言切换正常
- [ ] 搜索功能正常
- [ ] 快捷键正常
- [ ] 移动端适配
- [ ] 所有链接有效

### 兼容性测试
- [ ] Chrome
- [ ] Firefox
- [ ] Safari
- [ ] Edge
- [ ] 移动端浏览器

### 性能测试
- [ ] 页面加载时间 < 2s
- [ ] 搜索响应 < 100ms
- [ ] 无内存泄漏
- [ ] 无控制台错误

## 📊 监控指标

### 用户体验
- 页面停留时间
- 搜索使用频率
- 语言切换频率
- 跳出率

### 技术指标
- 页面加载时间
- 搜索响应时间
- API 调用次数
- 错误率

## 🔄 更新流程

### 日常更新

1. **更新版本信息**:
   - 修改 `index.html` 中的版本号
   - 更新 `update.md`

2. **更新特性亮点**:
   - 修改 `index.html` 中的 highlights
   - 更新 `search.js` 中的搜索索引

3. **更新翻译**:
   - 修改 `i18n.js`
   - 确保中英文同步

4. **测试并部署**:
   - 本地测试
   - 提交代码
   - 验证线上效果

### 大版本更新

1. **规划新功能**
2. **开发实现**
3. **全面测试**
4. **更新文档**
5. **发布 Release**
6. **通知社区**

## 🆘 常见问题

### Q: 语言切换不生效
**A**: 检查浏览器控制台是否有错误,确保 `i18n.js` 先于 `app.js` 加载

### Q: 搜索无结果
**A**: 检查 `search.js` 中的 `SEARCH_INDEX` 是否包含搜索词

### Q: 快捷键无效
**A**: 确保搜索功能已初始化,检查是否有其他脚本占用快捷键

### Q: 移动端显示异常
**A**: 检查 CSS 媒体查询,确保 viewport 设置正确

## 📞 支持

如有问题,请查看:
- `IMPLEMENTATION_SUMMARY.md` - 详细实现文档
- `test.html` - 功能测试工具
- GitHub Issues - 问题反馈

---

**版本**: v1.0
**最后更新**: 2025-12-31
